# Azure six-profile repeat campaign, 2026-08-27

This directory records the acceptance campaign that starts from public `main` at `7e6d1039acaa421c96d54435ea4f143a050f3736`.
The GitHub API and the clean local checkout independently resolved `refs/heads/main` to that commit before any campaign work began.

The campaign is not accepted by this kickoff record.
Acceptance requires two consecutive live rounds, each exercising all six configured Pi provider profiles, an Azure secondmate and its Azure child, the Azure-backed no-mistakes production path, a concurrent exact-head Crosscheck, complete result return, ordinary release, and zero task-resource cleanup.
The completed machine-readable evidence and claim map will replace this in-progress statement only after both rounds satisfy that contract directly.

`docs/azure-requirements.md` remains the acceptance authority.
